// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { firebaseConfig } from "../notForShare/firebaseConfig";
// Initialize Firebase
export const fbdb = initializeApp(firebaseConfig);